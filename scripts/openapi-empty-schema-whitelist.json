{
  "$comment": "PK-15 (DEEP_REVIEW 0ef3bbe) empty-schema whitelist — named components.schemas in apps/admin-api/openapi.json that are type:object with an EMPTY properties object (example-only / no documented fields). Guarded by the api-types-drift job in .github/workflows/ci.yml: any schema found empty that is NOT listed here fails CI. PK-02 (DEEP_REVIEW 0ef3bbe) landed: @nestjs/swagger plugin enabled in nest-cli.json, all Update* DTOs migrated to PartialType(CreateXxxDto) from @nestjs/swagger, @ApiProperty decorators added to bare class-validator DTOs. The 8 Update*/Create/Upsert entries (UpdateUserDto, UpdateTaskDto, UpdateApplicationDto, UpdateExecutorPackageDto, CreateProjectDto, UpdateProjectDto, UpsertProjectMemberDto, UpdateProjectMemberDto) were removed — their schemas now have proper properties. The 6 entries below are genuinely empty (entities/example-only/not DTO classes) and remain whitelisted.",
  "whitelist": [
    "Object",
    "SaveAiConfigDto",
    "RolloutStrategyDto",
    "TaskTemplate",
    "EventSubscription",
    "CreateApiKeyDto"
  ]
}
