# AutoCodeFlow UX 评审报告

> 作者：开发者视角全面评审
> 日期：2025-01
> 目标：以应用开发者和使用者身份，评估易用性、使用体验、执行器安装注册流程是否符合主流产品操作习惯

---

## 一、总体评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 整体导航与布局 | 7/10 | 侧边栏结构清晰，但菜单层级和入口顺序不够直觉 |
| 执行器安装体验 | 6/10 | 向导流程完整，但前提依赖（需先上传安装包）体验差 |
| 任务创建与配置 | 7/10 | 字段完整，但缺乏实时验证和内联帮助 |
| 执行器注册流程 | 5/10 | 全命令行操作，无 GUI 引导，新用户障碍高 |
| 错误提示与反馈 | 6/10 | 部分错误只有 message.error，缺乏上下文引导 |
| 空状态设计 | 5/10 | 多处空列表无引导 CTA，用户不知下一步 |
| 文档可达性 | 4/10 | 文档在 docs/ 目录，后台内没有任何文档链接入口 |
| 移动端适配 | 5/10 | 部分页面使用固定宽度，手机访问体验较差 |

---

## 二、问题明细

### P0 — 严重体验问题（阻塞正常使用）

#### UX-01：执行器安装向导依赖「先上传安装包」，但后台没有上传入口引导

- 现象：进入安装向导 Step 0，若未上传安装包，提示「暂无可用安装包，请先在后台上传」，但用户不知道在哪里上传。
- 影响：首次使用用户完全被卡住，无法完成执行器注册。
- 期望：空状态时显示跳转到「上传安装包」的按钮，或在向导内集成上传步骤。

#### UX-02：执行器实际注册流程文档缺失，后台没有任何命令行引导

- 现象：用户得到 curl 安装命令后，执行器安装完成的后续配置（配置 .env、启动服务、验证注册成功）全靠自己摸索。
- 影响：安装脚本执行后执行器不自动出现在列表，用户不知是成功了还是失败了。
- 期望：安装向导增加 Step 3「等待执行器上线」的轮询状态页，检测到新执行器后自动跳转到其详情页。

#### UX-03：登录后默认落地页是「数据看板」，全空状态无任何引导

- 现象：新安装的系统，仪表板显示全部为 0，没有「快速开始」引导，用户不知道第一步该做什么。
- 期望：空状态时显示「快速开始」卡片（创建第一个任务 → 注册执行器 → 触发执行）。

---

### P1 — 重要体验问题（明显影响效率）

#### UX-04：侧边栏菜单顺序不符合使用频率

- 现状顺序：数据看板 → 应用管理 → 任务管理 → 执行记录 → 执行器管理 → 包仓库 → 通知配置 → 用户管理 → 审计日志
- 问题：新用户第一步最需要的「执行器管理」排在第 5 位；「任务管理」是核心操作但排第 3 位。
- 期望：数据看板 → 任务管理 → 执行器管理 → 执行记录 → 应用管理 → 包仓库 → 通知配置 → 用户管理 → 审计日志

#### UX-05：执行器列表页「安装新执行器」按钮不显眼

- 现象：「安装向导」入口在列表右上角，是个普通 Button，和「刷新」并排，视觉权重相同。
- 问题：执行器列表为空时，没有居中的空状态 CTA，用户容易忽略安装入口。
- 期望：空状态时显示居中的大图标 + 「安装第一个执行器」主操作按钮；有数据时保持右上角按钮但改为 primary 样式。

#### UX-06：任务创建表单字段过多，缺乏分组和折叠

- 现象：TaskFormPage 将所有字段（基础信息、调度配置、执行配置、通知配置、高级配置）平铺在一个表单里。
- 问题：首次创建任务面对 20+ 个字段，不知道哪些必填，哪些可以先跳过。
- 期望：使用 Collapse 或 Tabs 将表单分为「基础」「调度」「高级」三组；必填字段高亮；非必填字段折叠默认收起。

#### UX-07：执行器注册成功后无法直观确认状态

- 现象：执行器上线后，列表中状态显示「在线」，但没有首次上线的通知/提示。
- 期望：首次注册时后台推送一条系统通知，或在执行器详情页显示「最后注册时间」。

#### UX-08：.env 配置错误时执行器静默启动失败，无前台提示

- 现象：executor-node/executor-python 若 ADMIN_API_URL 配置错误，心跳失败，后台日志不对外暴露，用户只能看到执行器一直不上线。
- 期望：执行器 GET /health 端点返回更详细的状态（admin-api 连通性、token 有效性），安装向导 Step 3 调用此端点做验证提示。

---

### P2 — 体验优化（影响使用流畅度）

#### UX-09：「快速触发」任务后无执行进度反馈

- 现象：点击「立即执行」后，仅弹出成功 Toast，用户需手动跳转到「执行记录」页才能看到执行状态。
- 期望：触发后 Toast 中带「查看执行详情」链接，或直接跳转到执行详情页。

#### UX-10：执行日志页面无自动滚动和实时刷新

- 现象：ExecutionDetailPage 日志区域加载后不自动滚动到末尾，执行中的任务也不实时刷新。
- 期望：任务执行中时，日志区域自动滚动到底部并每 3 秒轮询刷新；增加「实时跟踪」开关。

#### UX-11：包仓库页面功能入口不明显

- 现象：PyPI/npm 两个 Tab 内容较少，没有「如何发布包」的内联引导。
- 期望：每个 Tab 顶部添加一段「发布命令」代码块（可复制），降低首次使用门槛。

#### UX-12：通知配置测试发送无结果反馈

- 现象：NotificationSettingsPage 的「发送测试」按钮点击后只有加载状态，成功/失败提示不够明确。
- 期望：测试成功显示渠道名 + 绿色成功标记；失败时展示具体错误原因（如 Webhook URL 无效）。

#### UX-13：用户管理页面缺少「修改密码」快捷入口

- 现象：管理员只能通过编辑用户的方式修改密码，没有独立的「重置密码」按钮。
- 期望：每行用户添加「重置密码」操作，弹出简单 Modal 输入新密码即可。

#### UX-14：审计日志时间筛选器交互不够顺畅

- 现象：时间范围选择使用两个独立的 DatePicker，不如 RangePicker 直觉。
- 期望：改为 DatePicker.RangePicker，并添加「最近 1 天/7 天/30 天」快捷选项。

#### UX-15：页面间跳转缺少面包屑导航

- 现象：进入执行器详情、任务详情等二级页面后，无法通过面包屑快速回到上级。
- 期望：所有详情页顶部添加面包屑：数据看板 / 执行器管理 / 执行器详情。

---

### P3 — 小优化（锦上添花）

#### UX-16：代码块复制按钮使用 execCommand（已废弃 API）

- 现象：ExecutorInstallWizardPage 复制逻辑有 `document.execCommand('copy')` 降级分支。
- 期望：仅使用 `navigator.clipboard.writeText`，去掉已废弃的降级代码（现代浏览器已全面支持）。

#### UX-17：执行器详情页「性能图表」无数据时显示空白

- 现象：新注册执行器没有历史数据，图表区域显示空白坐标轴，没有「暂无数据」提示。
- 期望：图表无数据时显示 Empty 占位组件。

#### UX-18：任务列表搜索框无防抖

- 现象：每次输入字符都触发 API 请求。
- 期望：添加 300ms 防抖。

#### UX-19：登录页没有「记住我」选项

- 现象：每次关闭浏览器后需重新登录。
- 期望：添加「记住我」Checkbox，勾选后将 token 存入 localStorage 而非 sessionStorage。

---

## 三、执行器注册流程对比主流产品

| 对比项 | AutoCodeFlow 现状 | 主流产品标准（参考 Grafana Agent / Datadog Agent） |
|--------|-----------------|---------------------------------------------------|
| 注册方式 | 手动配置 .env + 手动启动 | 一行命令自动注册，平台生成唯一 Token |
| 安装引导 | 3 步向导（需先上传包） | 平台直接提供 curl 命令，无需预上传 |
| 注册反馈 | 无，需轮询列表 | 平台实时检测新 Agent 并提示「已检测到执行器」 |
| Token 管理 | 需用户复制 EXECUTOR_SECRET 到服务器 | 一次性 Token 内嵌在安装命令中自动配置 |
| 验证连通性 | 无 | 安装后 health check 自动回调平台验证 |
| 卸载/迁移 | 无 GUI 支持 | 提供 uninstall 命令和平台侧「删除」确认 |

核心差距：AutoCodeFlow 的安装向导生成了 curl 命令，但命令执行的安装脚本不在平台侧托管，且执行后没有回调机制确认注册成功，导致整个流程在「执行 curl」和「看到执行器上线」之间有一个黑箱。

---

## 四、改进优先级矩阵

```
高影响  UX-01  UX-02  UX-03       → 立即修复
        UX-04  UX-05  UX-06

        UX-07  UX-08  UX-09  UX-10  → 本迭代完成
        UX-12  UX-13  UX-15

低影响  UX-11  UX-14  UX-16~UX-19  → 下一迭代
```
