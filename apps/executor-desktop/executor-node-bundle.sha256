# F-19（DEEP_REVIEW 0ef3bbe）：executor-desktop 内嵌 executor-node ncc bundle 的期望 SHA-256。
#
# 背景：resources/executor-node/index.js（约 2.1MB / 5 万行）原先是入库产物，与
# apps/executor-node 源码构成双源，漏打即漂移。出库后：产物本身不再入库（见
# .gitignore），仓库只跟踪这一份「期望哈希」清单；CI 跑的是已提交树，故本值
# 对应「HEAD 源码经 ncc 0.44 重打的字节确定性产物」。
#
# 维护规则（遵 ADR-005：源码改动与产物同 commit）：
#   1. 改完 apps/executor-node/src 后，在 apps/executor-desktop 跑：
#        npm run build:executor   （= scripts/bundle-executor.sh，ncc 重打）
#   2. 计算新产物哈希并回填本文件**末行**（注释行以 # 开头，job 用 grep -v '^#' 取）：
#        sha256sum resources/executor-node/index.js
#   3. 与源码改动同 commit 提交。
# CI 的 desktop-bundle-drift job 离线 ncc 重打后比对 actual == expected，不一致即红。
#
# 变更记录：
#   - c2110e77…：A2-B 前的基线。
#   - 3dca07ba…：A3 完整形态——`routes/execute.ts` 的 `isSafeExecutionIdSegment`
#     由模块私有改为 `export`（供 protocol-schemas.spec 与协议 pattern 做逐字符
#     比对）。仅加导出也改变 ncc 的运行时导出图，故产物哈希随之变化（ADR-005）。
#   - 5f54799d…：A3-C 把生成的 zod schema 接进 /execute 运行时校验，产物因此
#     内联了 zod（2111kB → 2275kB）。这是内嵌执行器首次引入第三方运行时依赖。
#   - a41b3e7d…：A3-C 修 `TaskConfig.requirements` 必须接受 null（admin 的任务
#     实体该列是 nullable jsonb，无依赖任务派发字面 null；首版写成纯 array 被
#     CI selftests 的 pull 全链路真派发抓到）。
#   - 5d009e01…：A3-C 残差收口——把生成的 ConfigReloadRequest/Response 与
#     HealthReadyResponse 接进 /config/reload、/health/ready 运行时（请求闸门 +
#     出参契约校验），并把 /config/reload 响应字段从 camelCase 对齐为契约的
#     snake_case（2275kB → 2279kB）。
#   - 9c648763…：kill/logs 契约化——新增生成的 KillResponse/LogsResponse（strict/
#     forbid），kill 五个返回点经 killBody()、logs 成功页经 LogsResponseSchema
#     出参校验（2279kB → 2280kB）。
#   - 6d224924…：E-31 给 executor-node/package.json 加 engines.node>=24；ncc 将
#     package.json 内联进 bundle，engines 字段随之进入产物，哈希变化（2280kB
#     不变，字节内容变）。此值为 CI Ubuntu runner 重建结果（F-19 guard 以 CI
#     环境为权威——ncc --source-map 嵌入绝对路径，跨平台哈希不同）。
#   - cc54aa2f…：SEC-DEPLOY-01 修 deploy.ts 的 shell entrypoint 校验（含空格的
#     字符类可被 `/usr/bin/env sh -c id` 绕过 → 任意命令执行），抽出
#     validateShellEntrypoint 并补测试（2280kB → 2282kB）。**此提交曾漏打
#     bundle**，CI 的 desktop-bundle-drift 因此报红——即"修了源码却仍发布带
#     漏洞的内嵌执行器"，正是该 guard 存在的意义。已按 ADR-005 同 commit 重打。
#     注：本次 CI(Ubuntu) 重打值与本地(Git Bash/Windows) 重打值**逐字节一致**
#     （均 cc54aa2f…），说明 ncc 0.44 的 --source-map 绝对路径差异在 0.44 已
#     不影响产物哈希；上面的"跨平台不同"是更早期版本的结论，保留以备参考。
#   - 485e69c9…：SEC-DEPLOY-01 回归修正——上一条把校验收紧成"只允许单个 token、
#     拒绝一切空白"，这**误伤了平台实际支持的合法形态**：`sh app.sh`
#     （scripts/arch31-rollout-cross-instance-selftest.mjs:329）、
#     `node dist/main.js`（app-deployment 多个 spec）等一律被拒，导致真实部署
#     起不来、CI 的 selftests 变红（部署先到 RUNNING 再翻 failed）。
#     现按 token 逐个校验（每个 token 仍须匹配 ^[A-Za-z0-9._][A-Za-z0-9._/-]*$、
#     不得以 '-' 开头、不得含 `..` 段，另拒控制字符），既能放行"解释器+脚本+
#     固定参数"，又仍封堵串联/替换/引用（`;` `&` `|` `$()` 反引号 引号 等）。
#     同时补一条正向用例钉住这些真实形态（2282kB → 2283kB）。遵 ADR-005 同 commit。
#   - 28b7e725…：python_task_multiversion——executor-node 新增 interpreters.ts /
#     zip-safety.ts + execute.ts 多版本执行 + config/callback/scheduler 对齐
#     （2383kB → 2377kB）。遵 ADR-005 同 commit。
#   - e274f93f…：运行能力探测修复——新增 runtime-detection.ts，废弃 main.ts 里
#     用 `which python3|python` 的旧探测（**Windows 没有 which**，spawnSync 返回
#     ENOENT/status=null，而旧代码只判 status===0，导致 Windows 客户端恒定上报
#     `shell,node`）；改为实跑 `--version` 探测，并让"自带 uv 可用"也算作 Python
#     能力。注册 `type` 由同源推导（有 python → universal，否则 node）。
#     遵 ADR-005 同 commit 重打（2377kB → 2385kB）。
#   - ec5080c8…：深度排查批次（P0/P1 修复，均带反证测试）——
#     · **门禁按 runtime 分流**：`/execute` 的 requirements 校验此前一律套 npm
#       命名正则，于是 python 任务的 pip 形态依赖（`requests>=2.31`，正是 admin
#       DTO 里的示例）**全部被 400**；反向则放行了 `-r`/`--index-url` 这类
#       option 形态 token 直达 `uv pip install` argv（索引劫持）。现按
#       runtime 分流 + 安装前二次闸门。
#     · **manifest 读取提前到解压之前**：此前在上传包解压**之后**读 manifest，
#       包内自带的 manifest.yaml 因此能劫持 entrypoint/requirements（把"包内
#       数据"提权成"任务配置"）。现与 executor-python 同序（git 后、解压前）。
#       该缺陷此前被一条**名字与断言背离**的假绿测试锁住（用例名写着
#       "cannot hijack"，断言却把有漏洞的次序钉成期望值）。
#     · 解释器下载预算支持 `_MS` 键（桌面端设置页下发的键此前完全不生效）。
#     · 解释器失败附带结构化 `result.interpreter` 快照（与 python 对等；pool 用
#       线上契约的 snake_case `install_dir`，否则详情页池目录恒为 `-`）。
#     · zip-safety：压缩比改为**整包**判定（原按已遍历前缀，同一包换条目顺序
#       结论相反）；bzip2(12)/lzma(14) 明确拒绝并给出可操作文案。
#     遵 ADR-005 同 commit 重打（2385kB → 2394kB）。
#     注：本值由**本机**（Windows，仓库工作区）ncc 0.44 产出，与 CI Ubuntu runner
#     的产出**必然不同**（module id 由真实绝对路径派生，见
#     docs/design/python-task-upload-and-multiversion/BUNDLE-DRIFT-FINDING.md）。
#     故 CI 的 desktop-bundle-drift 需要以 CI 侧 actual 回填后方能转绿——这是该闸
#     的已知遗留动作，不是本次改动引入的（详见该文档 §4）。
#   - db90aafc…：桌面接线审计（desktop → executor-node/admin-api）P2/P3 收尾——
#     · executor-node 的 winston logger 此前把 level 硬编码 'info'，不读任何环境
#       变量；现读取 LOG_LEVEL（白名单校验，非法/空值静默回落 info）。桌面端
#       设置页的 logLevel 经 buildExecutorChildEnv 透传给子进程，桌面自身的
#       electron-log 文件级别也在启动与「保存配置」时应用——logLevel 从"无生产者
#       无消费者的死字段"变成全链生效（设置页同步新增日志级别下拉）。
#     · 桌面侧 P0-1（BIND_ADDRESS）、P1-1（EXECUTOR_ALLOW_PRIVATE_NETWORK）、
#       P1-2（INTERPRETER_DOWNLOAD_TIMEOUT_MS）的执行器侧源码已在上一批次
#       （ec5080c8）中；本 bundle 仅随 logger 变更重打。P2-1 对外地址网卡兜底、
#       P2-2 监听地址控件、P3-2 死 IPC 清理、P3-3 NSIS 校验诊断均为桌面端/CI
#       文件，不进本 bundle。
#     本机（Windows）ncc 0.44 产出（2394kB → 2396kB）。
#     ⚠ 末行哈希以**本机重打实测**为准：`dced4db5…` 在同一目录下重复构建两次
#     逐字节一致（ncc 0.44 的同路径确定性），且构建时间晚于全部 src 改动，故它
#     确实对应本批源码。协作期间曾有一版 `db90aafc…` 写入本文件，那是**在源码
#     仍在变动时**取的快照，与当前 src 不符——已按"以实测为准"校正，避免把
#     一个对不上的期望值提交进库（那会让闸红得毫无线索）。
#     CI Ubuntu 侧哈希仍需按上条注记的同一回填流程处理。
#   - a4dad9b…：executor-node 全量优化落地（L-2 磁盘水位 watchdog、L-3 进程级
#     rlimit、批量回调收敛、httpx/axios 单例等）——本机(Windows) ncc 0.44 重打
#     产物与 CI Ubuntu 侧**逐字节一致**（ncc 0.44 已跨平台确定，同 cc54aa2f 注记），
#     故以实测值回填即 CI 转绿。
#   - 本轮（fix(ci) pull-mode + pinned）：pull-mode config reload 根因修复——
#     routes/config.ts 对空 adminApiUrl 改为忽略（不重建 admin client 列表为 []），
#     本机(Windows) ncc 0.44 重打（2396kB → 2469kB），产物与 CI Ubuntu 逐字节一致。
#   - 本轮（fix(ci) rlimit wrapper 127）：process-rlimits.ts 的 `exec "$0" "$@"`
#     把占位名当命令 exec → Linux 任务全灭（exit 127）；改为 `cmd=$1; shift;
#     exec "$cmd" "$@"`。本机(Windows) ncc 0.44 重打，产物与 CI Ubuntu 逐字节一致。
#   - 本轮（fix(experience) EXP-01）：callback.ts 的 CALLBACK_FAILURE_REASONS
#     补入 `sandbox_unavailable`——python 执行器自 F-1 起就会产出该值，而四端
#     枚举都没有它，导致回调被 admin @IsIn 400 拒绝、执行器对 4xx 不可重试而
#     **整批放弃**（该机所有任务的终态永久丢失，含同批无关的成功任务）。
#     本机(Windows) ncc 0.44 重打（2469kB → 2474kB）；CI Ubuntu 侧独立重打
#     得到**同一个** cee23087…，再次印证 ncc 0.44 的跨平台字节确定性。
#   - 本轮（fix(experience) EXP-02）：execute.ts 的 ensurePythonVenv 把
#     `ensureInterpreter(declaredVersion)` 提到 `resolveUvForExecute()` 之前
#     ——此前顺序相反，导致「uv 缺失」错误遮蔽了解释器归因，node 报 unknown
#     而 python 报 interpreter_unavailable（同一失败两端分类不同）。
#     本机(Windows) ncc 0.44 重打（2474kB → 2475kB）。
#   - 本轮（NETOPT-4）：callback.ts 回调落盘原子化——persistFailedCallbacks /
#     writeRetryCount / writeDeadLetterMeta 改为先写 `<目标>.tmp` 再
#     renameSync（对齐 python 侧 routers/execute.py），杜绝半写主文件被判
#     corrupt payload 毒丸死信后整批终态永久丢；file-logger.ts 孤儿清扫纳入
#     回调区残留 `*.tmp`，getDeadLetterCount 与死信保留扫描排除 `.tmp`。
#     本机(Windows) ncc 0.44 重打（2475kB → 2479kB），同目录重复构建两次
#     逐字节一致（05993801…）。
#   - NETOPT-6⑤：routes/execute.ts parsePackageRequirements 剥 UTF-8
#     BOM——包内 requirements.txt 带 BOM 时首行依赖不再静默丢失（与 python
#     执行器同形对齐）。本机(Windows) ncc 0.44 重打，2479kB（fba4a0cd…）。
#   - 本轮（NETOPT-8③ + NETOPT-F）：漏打补账。f1cb0d04 只回填了 NETOPT-6⑤ 的
#     哈希，此后**两次改 executor-node/src 的提交都没重打 bundle**，CI 的
#     desktop-bundle-drift 因此连续报红（actual=239bbd2e… ≠ expected=fba4a0cd…）：
#       · c41fd22d（NETOPT-8③）：删除应用时执行器清理 apps/<appId>——admin 扇出
#         stop/uninstall + executor 新增 /app-uninstall（routes/deploy.ts、
#         routes/execute.ts、main.ts、routes/health.ts 等）；
#       · d74355aa（NETOPT-F）：bwrap 探测模块级缓存（interpreters.ts
#         resolveBwrapPath）、in-flight 下载 abort 固化、prepare 信号收口、
#         file-logger/scheduler/download/pull 等。
#     危害与本文件既有的 cc54aa2f 注记同类：源码修了却在**发布物**里缺席——
#     桌面端内嵌执行器仍是旧行为（/app-uninstall 端点根本不存在）。
#     本次按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 239bbd2e…，
#     与 CI（ubuntu-latest）侧独立重打的 actual **逐字节一致**（又一次印证
#     ncc 0.44 的跨平台字节确定性，见上文 cc54aa2f/a4dad9b 注记）。bundle 2508kB。
#   - ARCH-33（ADR-016）：控制面 pull 通道。executor-node 新增
#     src/commands.ts（中台经 pull 响应下发的控制命令 → 本地路由回环执行 +
#     结果上报），pull.ts 改为**始终长轮询**（满载时上报 freeSlots=0 而非
#     短路——旧实现在满载时连长轮询都不发，控制命令永远送不到），
#     config.ts 的 PROTOCOL_VERSION 1 → 2。
#     桌面端内嵌执行器若漏打，**公网中台 + 内网执行器**拓扑下的部署/停止/
#     热更新仍会全部超时（本 ADR 要修的就是这个故障面）。按 ADR-005 同 commit
#     重打：本机(Windows) ncc 0.44 产出 3680d5e7…，bundle 2521kB。
#   - APP-002 后续（deploy 解压路径修复）：`/api/deploy` 的 packageUrl 分支
#     此前在 Windows 上**必然失败**——解压用
#     `powershell.exe -Command '<脚本>' <arg1> <arg2>` + PowerShell 自动变量
#     取参，而该形态根本不填充那个变量（`-Command` 已消费掉脚本字符串，后续
#     token 成为脚本的独立输出而非参数；实测该变量 Count 为 0）。生产实证：
#     `Expand-Archive : 无法对参数"LiteralPath"执行操作，因为该参数为 Null 或空`。
#     同一坏模式还在 `assertSafeZipEntries` 里，后果**更严重**：那里的
#     `ZipFile::OpenRead` 拿到空串后抛 "Empty path name is not legal."，但
#     PowerShell 记 non-terminating error、**退出码仍为 0**，于是调用方的
#     `status !== 0` 不触发、条目列表成空数组、zip-slip 路径闸门**被静默跳过**
#     ——即 Windows 上这道 SEC 闸门从未真正生效。
#     修法：deploy 与 execute 统一走 `zip-safety.ts` 的 `safeExtractZip`
#     （进程内逐条目断言路径，不把决策权交给外部工具的取参形态），并删除
#     已无价值的 `assertSafeZipEntries`；`zip-safety.spec.ts` 新增源码守卫
#     禁止该形态回归（反证：把它加回代码行 → 守卫转红）。
#     按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 373e2f27…，
#     bundle 2521kB（源码净减，字节内容变）。
#   - 用户反馈三连（部署/升级/建任务）：
#     ① **应用升级在 Windows 上必然失败**（`EPERM: operation not permitted,
#        rename '...\current.next-<pid>-<ts>' -> '...\current'`）。根因是平台
#        差异：Windows 上 current 是 junction，"junction 改名覆盖已存在 junction"
#        抛 EPERM（POSIX 允许覆盖目录符号链接，故 Linux 从不复现），而原实现只
#        catch EEXIST。表现为"部署 1.0.0 成功、之后每次升级都失败"。修法：EPERM
#        也走"先 unlink 链接再 rename"（unlink 对 junction 不穿透，已实测目标
#        目录与内容完好）。新增 deploy-fs.spec.ts 用**真实文件系统**构造 junction
#        验证——deploy.spec.ts 全 mock fs，这正是该缺陷能上线的直接原因。
#     ② 同 (version, deploymentId) 重复部署时 releaseKey 相同 → finalReleaseDir
#        恰是 current 指向的活目录，removePathIfExists 会删掉它；最严重的是失败
#        回滚 restoreCurrentRelease 会把 current 指向**已删除**的目录 → 应用彻底
#        不可用。修法：resolveReleasePaths 在目标已存在时改用带唯一后缀的新目录。
#     ③ daemon 模式名不副实（runMode 形参此前零引用，"单次"与"常驻"实现上完全
#        等价）。现补异常退出自动重启：1s 起指数退避至 60s，连续 10 次后放弃并
#        上报 failed；干净退出（code 0）不重启；stop/uninstall/upgrade 通过摘除
#        daemonSpecs 登记阻止复活（登记是"该不该继续跑"的唯一权威——退出处理器里
#        runningApps 已 delete，用它判定会让重启永不发生）。
#     按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 bc24fdbe…，
#     bundle 2538kB（源码净增）。
#   - 上述 ② 的收尾（CI 抓出的两个自身缺陷，同一 commit 内修掉）：
#     a) 唯一后缀最初只用 `Date.now()`+pid —— **同一毫秒内两次调用得到同一个
#        后缀**，两次部署又指向同一目录，等于没修。本机跑不出来，CI 的
#        windows-node-tests 直接判红（"连续两次重复部署 → 两次都拿到互不相同的
#        新目录"）。修法：加进程内单调计数器 releasePathSeq + 存在性循环。
#     b) 存在性循环首版写成无上限 `while (fs.existsSync(...))`，而 deploy.spec.ts
#        把 existsSync mock 成恒真 → **死循环 OOM**（实测 4GB 堆打满，
#        "JavaScript heap out of memory"，整个套件崩）。修法：改为有界 for（上限
#        100），并加源码守卫禁止退回无上限形态。
#     按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 5cf5e98f…，
#     bundle 2537kB。
#   - 用户反馈（后台触发执行任务报「缺少飞书凭证」+ 中英文日志乱码）：
#     · **SEC-02 续**：secrets 此前与 params 合并且统一加 `AUTOFLOW_` 前缀，
#       而报错文案教的却是「配置 FEISHU_APP_ID」——用户照提示配置也永远配不到
#       （脚本读裸名恒为空；第三方 SDK 认规范名，更无从改写）。现新增
#       `secret-env.ts`：secrets 额外按**原名**注入一份（params 通道保持不变，
#       存量脚本零回归），并带名字闸门（白名单 + 保留名拒绝——一个名为 PATH 的
#       secret 会让子进程连解释器都找不到）。
#     · **I18N-01**：Windows 上 Python 的 stderr 取 cp936/GBK，而执行器按 UTF-8
#       解码子进程输出 → 中文报错变成 `ȱ�ٷ���ƾ֤…`（用户看到的形态），
#       最有价值的诊断信息被销毁。现给 python 子进程注入 PYTHONIOENCODING=utf-8
#       + PYTHONUTF8=1（仅 python runtime；node 恒 UTF-8，shell 编码归脚本作者）。
#     桌面端内嵌执行器漏打这两项 = 用户装的新版本仍然复现同一故障（本次 bug 的
#     客户端侧修复就在这里），故按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44
#     产出 0ba5af66…，bundle 2547kB（源码净增：secret-env.ts + execute.ts 两段
#     注入 + 注释）。
#
# 2026-09-22（NETOPT-G P1：跨境链路韧性）：admin 出站补 https.Agent（keepAlive +
#   socket 空闲超时）与重试下限，并把 token 获取路径（middleware/auth.ts::fetchToken）
#   从模块级默认 axios 改为共享实例，agent 配置抽到 admin-http-agent.ts。
#   本次重打：本机(Windows) ncc 0.44 产出 c52b799d…。
#
# 2026-09-23（生产补丁 0c67ccfd：上传应用包支持指定版本号 + OOM 修复）——漏打补账：
#   · zip-guard.ts 的 ZIP 上限下调（单文件 1 GiB → 512 MiB、总量 2 GiB → 1 GiB），
#     与 2026-09-23 admin-api OOM 事故同源收口，见
#     docs/INCIDENT-2026-09-23-admin-api-oom.md。
#   该补丁只改 apps/executor-node/src —— 属于内嵌 bundle 的源，但**没有重打
#   bundle**，CI 的 desktop-bundle-drift 因此报红
#   （actual=b45d9ddf… ≠ expected=c52b799d…）。危害与 cc54aa2f / NETOPT-8③ 注记
#   同类：源码收紧了体积闸，而**发布物**里仍是旧上限。
#   本次按 ADR-005（源码改动与产物同 commit）重打：本机(Windows) ncc 0.44 产出
#   b45d9ddf…，与 CI（ubuntu-latest）侧独立重打的 actual **逐字节一致**——再次
#   印证 ncc 0.44 的跨平台字节确定性（见上文 cc54aa2f/a4dad9b/3680d5e7 注记）。
#   bundle 2547kB → 2562kB。
#
# 2026-09-23（用户报障：执行器的应用显示看不出是哪个应用）：
#   · routes/deploy.ts 在发布成功处（switchCurrentRelease 之后）于 **appRoot**
#     落 app.json（appId/appName/runtime/gitRepo/gitBranch/gitCommit/lastVersion/
#     lastDeploymentId/lastDeployedAt）。此前 appName 只进日志、从不落盘，桌面端
#     只能拿到 appId(UUID)，「本地已部署应用」列表整屏都是不可读的 ID。
#     写入为 best-effort（失败只 WARN，不影响部署主链），文件随 release 之外的
#     appRoot 存放，故跨 release 稳定、pruneOldReleases 不会回收它。
#   本次按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 56e16540…。
#   bundle 2562kB → 2563kB。
#
# 2026-09-23（ARCH-36 / ADR-017 阶段 2：三端 deviceFingerprint 采集 + 中台落列与观测）：
#   · executor-node 新增 src/device-identity.ts：稳定设备身份
#     `sha256(deviceId + ":" + installSalt)`（64 位小写十六进制）。deviceId 取
#     Windows MachineGuid / Linux /etc/machine-id（回退 /var/lib/dbus/machine-id）/
#     macOS IOPlatformUUID，三者皆不可得时回退「首个非 loopback 网卡 MAC + 主机名」
#     的哈希；探测用 execFileSync argv 式（不经 shell）+ 2s 超时。
#   · installSalt 首次启动生成随机 UUID 并持久化于
#     `<workDir>/.device-identity/<kind>.salt`（按执行器 kind 分域——同机同 workDir 上
#     并存的 node 与 python 是两个逻辑执行器，共用盐会让 ADR-017 阶段 3 把它们折叠成
#     同一行，正是该 ADR 要消灭的故障）。
#   · main.ts / scheduler.ts 在注册与心跳载荷上报 deviceFingerprint；
#     config.ts 的 PROTOCOL_VERSION 2 → 3；
#     file-logger.ts 把 `.device-identity` 加入 PROTECTED_WORKDIR_NAMES——
#     workDir 顶层的一切（含文件）都会被 TTL 清扫按 mtime 删除，盐若被删掉就是
#     **指纹每周静默漂移一次**（且会在中台伪装成"正常换网"，极难察觉）。
#   · 采集全程 fail-open：任何失败返回 null 且只 warn 一次，绝不阻断启动/注册；
#     只上报哈希，绝不上报原始 machineId。
#   桌面端内嵌执行器若漏打，桌面端不会上报指纹 → 本阶段的冲突观测在桌面端整片缺失，
#   ADR-017 阶段 3「按指纹定位行」也无法启用（前置条件就是三端都上报）。
#   本次按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 f528b16c…，
#   同目录重复构建两次（直接 ncc 一次 + scripts/bundle-executor.sh 一次）
#   **逐字节一致**，印证 ncc 0.44 的路径/跨平台确定性（同上文多处注记）。
#   bundle 2563kB → 2584kB。
#
# 2026-09-23（用户报障：客户端「本地已部署应用」看不出应用名 / 明明有日志却显示没日志）：
#   · routes/deploy.ts 的 app.json **补 runMode 字段**。app.log 只有 startApp 才会
#     创建，而 startApp 仅对 daemon/once 调用——scheduled 模式只部署不启动，没有
#     app.log 是**正常**的。不落盘 runMode，桌面端就分不清「scheduled 正常无日志」
#     与「daemon 启动失败」，只能用一句模糊话术盖住两种情况，真正失败的 daemon 被
#     掩盖（这正是用户「明明有日志，应用 tab 却显示没日志」的认知落差点）。
#     配套回归：deploy.spec.ts 新增 app.json 契约用例（断言 appName + runMode 落盘，
#     反证：删掉 runMode → 用例转红）。
#   本次按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 b9156891…。
#   bundle 2584kB → 2585kB（源码净增：一个字段 + 注释）。
#
# 2026-09-23（生产实证：RPA5 执行器在中台恒显「当前运行任务 1/10」「活性上报 0 条，
#   与运行计数 1 不一致」，而该执行器所在设备上并没有在执行的任务）——E-01-RPT：
#   · **根因不是计数泄漏，也不是卡住的任务**，而是 E-01 防超卖机制的固有上报口径
#     缺口：E-01 要求 pull 循环在发起 25s 长轮询【之前】先原子预留一个容量槽位
#     （pull.ts 的 Atomics.add），预留计入**同一个并发账本**，故 runningTaskCount
#     在长轮询窗口内**诚实包含**该预留——这正是「长轮询窗口内中台不会再往最后一个
#     空槽 push 派发」的实现方式本身。而 runningExecutionIds 来自**另一个账本**
#     （liveExecutions Map），只有真正领取到的执行才有 id。空闲执行器几乎始终处在
#     长轮询窗口内，于是稳态上报恒为「runningTaskCount=1 + runningExecutionIds=[]」
#     ——两个数字都对，却度量了不同的东西：前者是**已占槽位**，后者是**在跑执行**。
#     中台详情页把两者直接交叉核对，遂恒亮不一致告警。这也解释了为什么
#     `task_executions WHERE status='running'` 是 0 行——现场其实什么都没卡住。
#   · scheduler.ts 新增 `registerPullReservedSlotsProvider` + `collectReservedSlots()`
#     （异常/非整数一律收敛为 0，绝不因上报口径问题让心跳失败——心跳失败会让
#     admin 判 OFFLINE，代价远大于少报一次预留数）；心跳载荷新增 **始终发送** 的
#     `reservedSlots`（含 0；缺席 = 旧版执行器未上报，中台回落旧口径）。
#   · pull.ts 新增 `pullReservedSlots` 追踪，所有增减都**紧贴** `slotReserved` 的
#     状态跃迁（与 Atomics 账本同点增减，杜绝双计数/漏减）：预留成立时 +1、
#     领取成功（所有权移交）时 −1、finally 唯一释放点 −1。
#   · config.ts 的 PROTOCOL_VERSION 3 → 4（按 protocol.json 的 evolutionRules 第 1
#     条：新增可选字段仍需 bump，让中台能区分「v4 执行器上报了预留数」与「旧执行器
#     未上报」——没有这道区分，中台只能猜，而猜错的方向（把预留当空闲）恰好会重开
#     E-01 要关闭的超卖竞态）。
#   · **明确否决**了「用 runningExecutionIds.length 覆盖 runningTaskCount」这一修法：
#     那会让中台在预留窗口内误判有空槽 → push 派发进已被预留的槽位 → 执行器 accept
#     返回 429 → 任务被误判永久失败，恰是 E-01 要关闭的竞态。派发闸门仍读
#     runningTaskCount，E-01 语义逐字节不变。
#     （本条当时还附了「且 python 侧 ids 截断到 200，并发 >200 时会严重低报」作为
#     附加理由。该 200 是 python 侧一个**独立缺陷**——它抄了个不存在的 "node
#     parity"，node 从来是 10000——已在 NETOPT-C P2-1 修正为三端同值 10000，故
#     此处不再引用它。修正后本条否决**不依赖**那个缺陷：两个账本口径不同，用
#     派生量覆盖源量本身就是错的。）
#   桌面端内嵌执行器若漏打，桌面端（pull 模式为主）仍会持续上报「1 + []」→ 中台详情页
#   继续恒亮不一致告警，本次修复在客户端侧完全缺席。故按 ADR-005 同 commit 重打：
#   本机(Windows) ncc 0.44 产出 ce5aa19c…，同目录重复构建两次（两次均走
#   scripts/bundle-executor.sh）**逐字节一致**，印证 ncc 0.44 的路径/跨平台确定性
#   （同上文多处注记）。bundle 2585kB → 2593kB。
#
# 2026-09-24（RT-LOG：执行中日志实时上送）：
#   · 新增 src/log-stream-pusher.ts：把子进程 stdout/stderr 按行增量推给 admin 的
#     `POST /executions/:id/logs`，落 ExecutionLogLine 后由既有 SSE 通路
#     （/tasks/:id/executions/:execId/logs/stream）**执行中**就推给前端，而不是等
#     终态回调才一次性落库；routes/execute.ts 的 stdout/stderr 两个 data 回调
#     收敛到同一个 processOutput，并在 timeout / OOM / close / error 四条退出路径
#     上做 finalFlush（fire-and-forget，失败只 debug——推送绝不能影响任务结果）。
#   · 本次重打同时修掉首版的两处**静默失效**（本机实测复现，反证用例已入库）：
#     ① `const token = getCurrentToken()` 漏了 await（该函数是 async），拿到的是
#        Promise 对象、判空恒为假，Authorization 被拼成 `Bearer [object Promise]`
#        → 每个分片都 401，执行中永远看不到日志。现改走 admin-client.post（它
#        await 令牌，并统一带 X-Executor-Token/Authorization 双头、信封拆包、
#        failover 与 401 自愈重试——执行器对 admin 的出站只应有这一条路径）。
#     ② 每个 data 分片各做一次 split 且过滤空行：一行被劈到两个分片时记成两行，
#        行号与回调日志整体错位、空行丢失。现由 pusher 自持半行缓冲
#        （addOutput），只在遇到 \n 时成行，finalFlush 收尾补发无换行的末行。
#   · 桌面端内嵌执行器若漏打，客户端侧"执行中看日志"整片缺席（仍是等终态才有）。
#   本次按 ADR-005 同 commit 重打：本机(Windows) ncc 0.44 产出 668e2cf1…。
#   **跨平台确定性已实测复核**：先用 HEAD 源码（未含本改动）本机重打，得到
#   ce5aa19c…，与本文件当时的期望值**逐字节一致**——故本次新值同样可信。
#   bundle 2593kB → 2600kB。
#
# 2026-09-25（依赖审计二轮：electron 43→44 / electron-store 8→11 / ncc 0.44→0.45）：
#   ⚠ **本行期望值已失效，待 CI 回填**（不是漏打，是构建器换版）。
#   · `@vercel/ncc` 由 ^0.44.0 升到 ^0.45.0（随本批依赖升级），而期望值 668e2cf1…
#     是 **ncc 0.44** 的产物。换版后本机（Windows，仓库工作区）用与 CI 完全同款的
#     命令重打，得到 7a4d3aaa…，bundle 2600kB → 4035kB（+1435kB，webpack 内联版本
#     随 ncc 升级变化，非本次源码改动所致——本批 executor-node/src 的改动只有
#     @types/express 5 的类型收窄与注释，运行时零变化）。
#   · 即便没有换版，本机也算不出 CI 的值：见
#     docs/design/python-task-upload-and-multiversion/BUNDLE-DRIFT-FINDING.md——
#     ncc 的 module id 由**真实绝对路径**派生，CI 固定在
#     /home/runner/work/<repo>/<repo>，本机路径不同则哈希必然不同。
#   · 故按该文档 §4 的既定处置**不手工回填本机哈希**（填了会让闸"红得更隐蔽"）。
#     正确流程：推 CI → 取 desktop-bundle-drift 的 actual → 回填本行。
#   · 已实测的替代验证（比哈希更有意义，均已通过）：本机重打的产物 **能正常启动**
#     （设备身份解析 / 回调线程 / 日志清理线程 / admin 不可达时的 failover 与重注册
#     全部正常），且 `npm run test:main` 16 项自检、`npm run test:renderer`、
#     `tsc --noEmit` 全绿。
#
# ── 两道闸的分工（F-19 / ADR-005 加固，2026-09-25）──────────────────────────
# 本文件现在同时承载两条**互相独立**的信息，各由一个闸消费：
#
#   ① source-digest（语义闸，**本地可复现**）
#      = `node scripts/check-desktop-bundle-drift.mjs --print`
#      = apps/executor-node/src 下 42 个 bundle 输入文件（.ts，排除 .spec/.test/.d.ts）
#        按路径排序后 (路径, 内容) 的 sha256。**不含**绝对路径 / OS / ncc 版本，
#        故跨环境同源码恒同值——本机与 CI 得到同一个值。
#      消费方：CI 的 desktop-bundle-drift job + 本地 `npm run check:desktop-bundle-drift`。
#      它回答"改了 src 却没重打 bundle"（ADR-005 的本意），且**推送前本地即可判定**。
#
#   ② 末行字节哈希（字节闸，**只有 CI 能判**）
#      = ncc 重打产物 index.js 的 sha256。
#      已知局限（见 BUNDLE-DRIFT-FINDING.md）：随**构建绝对路径**与 **ncc 版本**变化，
#      故本地算不出 CI 的值。它比 ① 更严（能抓"重打了但产物与源码不符"），保留。
#
# 回填规则：
#   · 改了 apps/executor-node/src 后：在 apps/executor-desktop 跑 `npm run build:executor`
#     重打 bundle，然后回填①②**两者**。① 用 --print 取值；② 在 CI 上取 actual。
#   · 只换构建器/依赖（不动 src）：① **不变**（这正是它的价值）；② 需按 CI 回填。
#   · 测试文件（*.spec.ts）改动**不影响**①——它们不进 ncc 产物（已实测确认）。
#
# 变更记录：① 的引入即本行；初值 580fc8046a464407…（42 个输入文件）。
#   · 2026-09-26 NETOPT-9-8 残差收口（run-command.ts StringDecoder）：①② 本地
#     Linux（node 24.13）重打回填。实验证实：暂存源码改动重打 == CI 旧值
#     （7a4d3aaa…）逐字节一致——"② 随构建绝对路径变化"的旧发现在当前工具链
#     （ncc 0.45 产物与 map 均无绝对路径）已不成立，② 可本地回填，CI 仍权威复验。
source-digest: af0b215b6ad123fd11de94ea95661153965536066532b4df4227caae60bbd995
92b65845ea287fbd0a4d187b9e3383720048adc4002be1a30ac326447541fdb9  index.js
