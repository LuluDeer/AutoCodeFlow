chore(release-desktop): bump executor-desktop 1.5.2 → 1.5.3

为什么必须发这一版：desktop-v1.5.2 出包后，main 上又合入了 **5 个改动桌面端
代码的 commit，全部未进安装包**（已逐个确认在 tag 之外）：

  81e50fe  fix(review): 3 个 P0 + 多项 P1/P2（全部带反证）
  43ec969  fix(desktop): 心跳启动点漏传 adminApiUrl，中台直达探针静默失效（EXP-03）
  9a94481  fix(experience): 暗色不可读 / 告警色阶失效 / 复制假成功 / 桌面端按钮永久禁用
  e749c36  fix(desktop): Python 环境诊断在保存配置后不刷新（EXP-06）
  a4dad9b  feat(optimize): 全量优化（含配置脱敏 config-sanitize 新模块）

即用户手上的 1.5.2 缺这 5 个修复（含 2 个 P0）。自动更新链路虽已修好（PR #16
demote-sdk-release），但只能推下一个版本，故需发 1.5.3 把它们交付出去。

改了两处版本号（package.json + package-lock.json 的第 3/9 行），与上一轮
desktop bump 的形态一致；lockfile JSON 已校验合法。

注：apps/executor-desktop/README.md 里出现的 "desktop-v1.5.2" 是**历史踩坑
记录**（NSIS 校验需先取嵌套 app-64.7z），不是版本声明，保持不动。
